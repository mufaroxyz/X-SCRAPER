{
  "compilerOptions": {
    // Language & Version settings
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "node",

    // Directory settings
    "outDir": "dist",
    "rootDir": "src",

    // Type-checking settings
    "strict": true
  }
}
